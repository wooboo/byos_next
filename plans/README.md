# Implementation Plans

Generated by the `improve` skill on 2026-06-13. Execute in the order below unless dependencies say otherwise. Each executor: read the assigned plan fully before starting, honor its STOP conditions, and update your row when done.

The current Fallow gate is already clean for the branch (`fallow audit --base origin/main` reports `No issues in 18 changed files`). These plans address the remaining inherited Fallow findings that are still visible when the report lists excluded issues: 5 complexity findings and 3 duplication clone groups.

## Execution Order & Status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Extract shared calendar screen rendering helpers | P1 | M | - | DONE |
| 002 | Extract shared calendar data helpers | P1 | M | - | DONE |
| 003 | Lower image-processing CRAP with focused tests and small helpers | P2 | S | - | DONE |
| 004 | Split sidebar rendering branches and tooltip normalization | P2 | M | - | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency Notes

- 001 and 002 are independent: 001 touches calendar React screen rendering; 002 touches calendar `getData` helpers.
- 003 is independent and should not touch calendar or sidebar files.
- 004 is independent but has higher UI review risk than 003, so run it after the lower-risk plans if batching work.

## Current Fallow Evidence

Latest verification run:

```text
pnpm dlx fallow audit --base origin/main --format compact
```

Reported:

```text
No issues in 29 changed files
```

Retired findings:

- `components/ui/sidebar.tsx:154` `Sidebar`, cyclomatic 9, high.
- `utils/image-processing.ts:240` `detectEdges`, cyclomatic 8, high.
- `app/(app)/recipes/screens/calendar-monthly/getData.ts:31` `getData`, cyclomatic 8, high.
- `components/ui/sidebar.tsx:498` `SidebarMenuButton`, cyclomatic 5, moderate.
- `utils/image-processing.ts:196` `ditherBayer`, cyclomatic 5, moderate.
- Calendar rendering duplication across `calendar-daily.tsx`, `calendar-monthly.tsx`, and `calendar-weekly.tsx`.
- Calendar data filtering duplication across `calendar-monthly/getData.ts` and `calendar-weekly/getData.ts`.

## Findings Considered And Rejected

- Full rewrite of all calendar screens into one generic calendar component: rejected for now because daily, weekly, and monthly layouts have different information density and e-ink rendering constraints; plans 001 and 002 extract only the repeated primitives.
- Suppressing Fallow with `fallow-ignore` comments: rejected because the remaining issues are real maintenance hotspots and have straightforward extraction or characterization-test paths.
- Adding browser/E2E tests for sidebar before splitting it: rejected as first step because this repo's current test baseline is Node's built-in test runner under `tests/*.test.ts`; plan 004 keeps verification to lint/typecheck plus React structure-preserving extraction unless a UI test harness is introduced separately.
